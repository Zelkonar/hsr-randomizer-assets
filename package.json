{
  "name": "hsr-randomizer-infra",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "sync": "tsx scripts/sync.ts",
    "generate": "tsx scripts/generate-characters.ts",
    "upload": "tsx scripts/upload.ts",
    "deploy": "tsx scripts/deploy.ts"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@types/node": "^24.0.0",
    "sharp": "^0.34.0",
    "tsx": "^4.0.0",
    "typescript": "~6.0.0"
  }
}
